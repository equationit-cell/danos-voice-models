# DanOS Voice Models

Static hosting for large model assets used by the DanOS dashboard's
voice assistant (https://danos-dashboard.vercel.app). Hosted here via
GitHub Releases since the models exceed Vercel's 100MB per-file limit.

Release assets are plain, publicly downloadable files — no auth
required, fetched directly by the browser at runtime.
